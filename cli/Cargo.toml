[package]
name = "ami-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
git2 = "0.19"
anyhow = "1.0"
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
std = { version = "0.1", features = ["fs", "process"] }
